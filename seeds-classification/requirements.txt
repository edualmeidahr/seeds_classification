# Dependências do projeto (versões mínimas testadas).
# Instale com:  pip install -r requirements.txt
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
ucimlrepo>=0.0.7        # download do dataset direto do UCI (opcional; há fallback local)
joblib>=1.3             # salvar/carregar modelos treinados
tabulate>=0.9           # exportar tabelas em Markdown (DataFrame.to_markdown)
jupyter>=1.0            # executar o notebook
notebook>=7.0
nbformat>=5.9
# Opcional: se disponível, é usado automaticamente para o post-hoc de Nemenyi.
# Caso ausente, o projeto usa uma implementação própria baseada em scipy.
# scikit-posthocs>=0.7
